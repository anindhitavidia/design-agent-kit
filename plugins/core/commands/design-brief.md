---
description: Run only Stages 1 and 2 of the design sprint (research + brief + design spec). Stops before scaffolding code. Useful when you want to align on direction before committing to a prototype.
---

# /design-kit:design-brief

## Usage

`/design-kit:design-brief <project-name>`

## Behavior

Run Stages 1-2 of `design-sprint-runner`. Stop after `02-design-spec.md` is written and validated.

Print suggestion: `/design-kit:design-sprint <project-name>` to continue with Stages 3-4, or `/design-kit:prototype <project-name>` to run just Stage 3.
