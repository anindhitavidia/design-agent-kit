# Design System

> Read by `design-engineer`, `component-reviewer`, `qa-designer`, and stack profile skills.

## Component library

<!-- TODO: Name and import path of the design system package.
Example: `import { Button } from '@acme/design-system'` -->

## Component inventory

<!-- TODO: List the key components available. Example:
- Button (primary, secondary, ghost, destructive)
- TextInput, Textarea, Select, Checkbox, RadioGroup
- Alert, Toast, Dialog, Drawer
- DataTable, Pagination
- Badge, Tag, Avatar
-->

## Conventions

<!-- TODO: Key conventions agents should follow when building components.
Example:
- All form fields use the `Field` wrapper for label + error layout
- Loading states use `<Skeleton>` not `<Spinner>` except for page-level loads
- Colors come from CSS custom properties (`--color-primary`, etc.)
-->

## Tokens

<!-- TODO: Where are design tokens defined? Link or path.
Example: `src/styles/tokens.css` or Figma variables link -->
