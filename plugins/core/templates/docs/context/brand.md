# Brand

> Read by `brand-guardian`, `design-engineer`, `qa-designer`.
> Run `/design-kit:init` with a Figma file link or brand guidelines URL to auto-populate this file.

## Voice and tone

<!-- TODO: How should the product sound? Consider:
- Formality level (e.g. "Direct and professional, not stiff")
- Warmth (e.g. "Friendly but not chatty — we respect the user's time")
- Technical depth (e.g. "Assumes comfort with software tools; never condescending")
- What to avoid (e.g. "No jargon, no passive voice, no filler words like 'simply' or 'just'")

Example: "Clear and calm. We communicate like a knowledgeable colleague, not a support script.
We're precise about technical things and human about everything else." -->

## Palette

<!-- TODO: Describe the color philosophy. Reference token names from design-system.md — don't restate hex values here.
Example:
- Primary: used for actions and focus states. Conveys trust and reliability.
- Surface: neutral backgrounds. Light in light mode, dark in dark mode.
- Accent: used sparingly for highlights and badges. Never for interactive elements.
- Semantic: success (green), warning (amber), error (red) — standard meaning, don't subvert.
- We avoid: gradients on interactive elements, pure black/white, high-chroma decorative colors.
-->

## Typography

<!-- TODO: Describe the type philosophy.
Example:
- Body: readable at small sizes; used for all prose and UI labels
- Heading: same typeface, heavier weight — hierarchy through weight not size alone
- Mono: used for code, IDs, and technical values only
- We avoid: more than 2 font sizes in a single component, decorative scripts, all-caps for body copy
-->

## Layout and spacing

<!-- TODO: Describe spatial philosophy.
Example:
- Generous whitespace — we'd rather have breathing room than pack in features
- Consistent 8pt grid; no odd spacing values
- Cards use a consistent shadow level; we don't stack shadows
- Density: default to comfortable; offer compact mode for power users
-->

## Imagery and iconography

<!-- TODO:
- Icon style (e.g. outline, filled, rounded, sharp)
- Illustration style if used (e.g. flat, abstract, no humans)
- Photography guidelines if applicable
- What we avoid (e.g. stock photos, clipart, overly literal icons)
-->

## What we avoid

<!-- TODO: Anti-patterns that are explicitly off-brand.
Example:
- Dark patterns (hidden costs, deceptive defaults, guilt-trip copy)
- Overly playful UI in serious workflows (no confetti on data deletion)
- Decorative animations that don't communicate state
- Red for anything other than errors/destructive actions
- Marketing language inside the product UI ("Supercharge your workflow!")
-->
