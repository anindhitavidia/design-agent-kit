# Coding Rules

> Project-specific coding rules. Read by `code-reviewer` alongside `CODING_GUIDELINES.md`.
> `CODING_GUIDELINES.md` has the universal rules; put project-specific rules here.

## Stack-specific rules

<!-- TODO: Rules specific to your stack. Examples:
- Next.js App Router — pages are server components by default; add "use client" only when needed
- All data fetching in server components via `fetch()` with `cache: 'no-store'` for dynamic data
- API routes live in `app/api/` following RESTful conventions
-->

## Architecture rules

<!-- TODO: Rules about code organization. Examples:
- Business logic lives in `lib/`, not in components
- Shared types in `types/`, not co-located unless component-specific
- No circular dependencies between feature directories
-->

## Testing rules

<!-- TODO: Rules about testing approach. Examples:
- Unit tests for pure utility functions in `lib/`
- Integration tests for API routes
- E2E tests via Playwright for critical user flows
-->
